# Security Policy / 安全策略

AeMusic 目前处于公开预览阶段。我们重视可能影响用户账号凭据、本地数据、网络请求或播放链路的安全问题。

## Supported Version / 支持版本

当前仅对仓库默认分支上的最新公开 Preview 版本提供安全修复支持。旧的预览构建可能不会获得单独补丁。

## Reporting a Vulnerability / 报告安全问题

如果问题可能涉及以下内容，请不要首先在公开 Issue 中披露完整利用细节：

- Cookie、Token、密码、API Key、签名私钥或其他敏感凭据泄露；
- 可导致凭据发送到非预期主机的请求链路；
- WebView / URL 导航带来的高风险问题；
- 任意文件读写、路径穿越、代码执行或明显的数据破坏；
- 可以稳定影响其他用户账号或隐私的数据暴露问题。

优先使用 GitHub 仓库的 **Security → Report a vulnerability / Private vulnerability reporting** 功能（如仓库已启用）。如果该功能尚未启用，请通过仓库所有者公开提供的非敏感联系方式请求一个私密报告渠道；不要在公开 Issue 中粘贴真实凭据。

报告中建议包含：

1. 受影响的版本、文件或功能；
2. 可复现步骤和必要的环境信息；
3. 影响范围与前置条件；
4. 已进行脱敏的日志或截图；
5. 如适用，可提供最小化 PoC，但请移除真实账号数据与秘密。

## Secret Handling / 凭据处理

请始终使用测试账号、无效占位 Token 或脱敏值。即使凭据已经失效，也不建议提交到 Git 历史，因为历史记录可能长期可见或被镜像缓存。

如果发现真实秘密已经进入 Git 历史，应先在对应服务端撤销/轮换该秘密，再清理仓库历史；仅删除最新文件中的字符串并不足以完成泄露处置。

## Scope / 范围说明

第三方平台接口不可用、平台限频、账号验证码、版权/地区限制或第三方服务自身的安全问题，并不当然构成 AeMusic 安全漏洞；但如果 AeMusic 的实现扩大了凭据暴露、越权访问或远程内容风险，仍欢迎按上述方式报告。
