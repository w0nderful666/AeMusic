package com.aemusic.design.theme

import androidx.compose.material3.Shapes

/**
 * The unmodified Material 3 shape scale is the Pixel-native baseline.
 * AeMusic only overrides shapes at component call sites when music-specific identity requires it.
 */
val AeShapes = Shapes()
